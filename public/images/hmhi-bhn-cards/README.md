Replace the SVG placeholders in this folder with de-identified insurance card images.

Suggested workflow:

1. Keep the same filenames used in `src/app/university-of-utah-behavioral-health-coverage/cardExamples.ts`.
2. Export card images with clear text and no PHI.
3. After replacing images, update callout `top` and `left` values in `cardExamples.ts` so pins align.

Current placeholder files:

- `regence-uofu-front-example.svg`
- `regence-uofu-back-example.svg`
- `uuhp-uofu-back-example.svg`
